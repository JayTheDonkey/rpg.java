public abstract class RangedAttack extends Attack {
 public RangedAttack(String tempName, String tempDescriptor, boolean tempPlural, int tempDice, int tempAdds) {
  super(tempName, tempDescriptor, tempPlural, tempDice, tempAdds);
  }
}