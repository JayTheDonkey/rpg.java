// different than a weapon in every way imaginable
public class RangedWeapon extends Weapon {
  public RangedWeapon(String tempName, int tempDice, int tempAdds) {
    super(tempName, tempDice, tempAdds);
    }
  }